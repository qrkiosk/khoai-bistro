import React from "react";
import { Box, Text } from "@chakra-ui/react";

const EmptySearchParams = () => {
  return (
    <Box>
      <Text>
        Đường dẫn vào ứng dụng không hợp lệ, vui lòng kiểm tra lại QR code.
      </Text>
    </Box>
  );
};

export default EmptySearchParams;
